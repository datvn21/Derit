FROM node:22-alpine

WORKDIR /app

# Install Java (JDK 11), Python 3, and nsjail for sandboxing
RUN apk add --no-cache openjdk11 python3 bash nsjail

COPY package.json ./
RUN npm ci

COPY . .

EXPOSE 3000

ENTRYPOINT ["tini", "--"]
CMD ["nodemon", "index.js"]
